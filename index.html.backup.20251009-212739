<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CodeCraftMD | AI-Powered Medical Billing Automation</title>
  <meta name="description" content="AI-powered medical billing and ICD-10 code extraction platform for healthcare professionals">
  <meta name="theme-color" content="#1D4ED8">
  <meta name="color-scheme" content="light">
  <link rel="manifest" href="/manifest.json">
  <link rel="icon" type="image/png" sizes="192x192" href="/icons/icon-192x192.png">
  <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/mobile-enhancements.css">

  <!-- Gumroad Script -->
  <script src="https://gumroad.com/js/gumroad.js"></script>

  <!-- Firebase SDKs - Latest Version -->
  <script src="https://www.gstatic.com/firebasejs/10.14.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/10.14.0/firebase-auth-compat.js"></script>

  <!-- Firebase Configuration -->
  <script>
    const firebaseConfig = {
      apiKey: "AIzaSyDxAdkFfUtpZzgZkEwN0389fh7Bak3oe4E",
      authDomain: "codecraftmd-e48b0.firebaseapp.com",
      projectId: "codecraftmd-e48b0",
      storageBucket: "codecraftmd-e48b0.appspot.com", // ✅ fixed typo
      messagingSenderId: "410377358686",
      appId: "1:410377358686:web:ba8f33b03827281e5993cf"
    };
    firebase.initializeApp(firebaseConfig);
  </script>
</head>
<body class="bg-white text-gray-800">

  <!-- Lead Magnet Popup -->
  <div id="leadMagnetModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden items-center justify-center p-4">
    <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full p-8 transform transition-all duration-300 scale-95 opacity-0" id="modalContent">
      <!-- Close Button -->
      <button id="closeModal" class="absolute top-4 right-4 text-gray-400 hover:text-gray-600 text-2xl font-bold">
        ×
      </button>
      
      <!-- Modal Content -->
      <div class="text-center">
        <!-- Icon -->
        <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C20.832 18.477 19.246 18 17.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
          </svg>
        </div>
        
        <!-- Headline -->
        <h3 class="text-2xl font-bold text-gray-900 mb-3">Free Guide: Cut Your Billing Time by 80%</h3>
        
        <!-- Subheadline -->
        <p class="text-gray-600 mb-6 leading-relaxed">
          Discover the 5 proven AI strategies that help healthcare professionals eliminate billing busywork and focus on patient care.
        </p>
        
        <!-- Benefits List -->
        <ul class="text-left text-sm text-gray-600 mb-6 space-y-2">
          <li class="flex items-center">
            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
            Automated ICD-10 code extraction
          </li>
          <li class="flex items-center">
            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
            Smart CPT code matching
          </li>
          <li class="flex items-center">
            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
            Intelligent claims pre-screening
          </li>
          <li class="flex items-center">
            <svg class="w-4 h-4 text-green-500 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
            </svg>
            Real implementation roadmaps
          </li>
        </ul>
        
        <!-- CTA Button (Gumroad Link - to be updated with actual product URL) -->
        <a href="#" id="gumroadButton" class="gumroad-button bg-blue-600 text-white font-semibold px-8 py-4 rounded-xl hover:bg-blue-700 transition shadow-lg block text-center mb-4">
          Get Your Free Guide →
        </a>
        
        <!-- Trust Badge -->
        <p class="text-xs text-gray-500">
          📧 Instant download • No spam • Created by Dr. Chukwuma Onyeije
        </p>
      </div>
    </div>
  </div>

  <!-- Navbar -->
  <header class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <!-- CodeCraftMD Professional Logo -->
      <div class="flex items-center">
        <svg class="w-10 h-10 mr-3" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
          <!-- Medical Cross Background -->
          <rect width="32" height="32" rx="6" fill="#1D4ED8"/>
          <!-- Cross -->
          <rect x="14" y="6" width="4" height="20" fill="white" rx="1"/>
          <rect x="6" y="14" width="20" height="4" fill="white" rx="1"/>
          <!-- Code Brackets -->
          <path d="M10 10L8 12L10 14" stroke="#60A5FA" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M22 10L24 12L22 14" stroke="#60A5FA" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <div>
          <h1 class="text-2xl font-bold text-blue-700" style="font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; letter-spacing: -0.5px;">CodeCraftMD</h1>
          <p class="text-xs text-gray-500 -mt-1" style="font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;">Medical Billing Automation</p>
        </div>
      </div>
      <nav>
        <a href="#features" class="mx-4 text-sm font-medium hover:text-blue-600">Features</a>
        <a href="#how-it-works" class="mx-4 text-sm font-medium hover:text-blue-600">How It Works</a>
        <a href="knowledge-hub/index.html" class="mx-4 text-sm font-medium hover:text-blue-600">Knowledge Hub</a>
        <a href="#contact" class="mx-4 text-sm font-medium hover:text-blue-600">Contact</a>
        <a href="#" class="ml-6 bg-blue-700 text-white px-4 py-2 rounded-lg hover:bg-blue-800 text-sm">Get Started</a>
      </nav>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="bg-white py-24">
    <div class="max-w-5xl mx-auto px-6 text-center">
      <h2 class="text-5xl font-bold text-gray-900 leading-tight mb-6">
        Transform Clinical Notes into Reimbursement-Ready Codes
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-10 leading-relaxed">
        CodeCraftMD leverages AI to extract ICD-10 codes and generate billing summaries—fast, accurate, and HIPAA-aligned.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#" class="bg-blue-600 text-white px-8 py-4 rounded-xl text-lg font-semibold hover:bg-blue-700 transition shadow-sm">
          Start Your Free Trial →
        </a>
        <button onclick="window.showLeadMagnet && window.showLeadMagnet()" class="bg-green-600 text-white px-8 py-4 rounded-xl text-lg font-semibold hover:bg-green-700 transition shadow-sm">
          📚 Free Guide: Cut Billing Time 80%
        </button>
      </div>
    </div>
  </section>

  <!-- Login Section -->
  <section class="py-16 bg-white">
    <div class="max-w-md mx-auto bg-white p-8 rounded-xl shadow-sm border border-gray-100">
      <h2 class="text-2xl font-bold text-gray-900 text-center mb-6">Login or Sign Up</h2>
      <input id="email" type="email" placeholder="Email" class="w-full mb-4 p-3 border border-gray-200 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-100 outline-none transition" />
      <input id="password" type="password" placeholder="Password" class="w-full mb-6 p-3 border border-gray-200 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-100 outline-none transition" />
      <button onclick="signUp()" class="w-full bg-blue-600 text-white py-3 rounded-lg hover:bg-blue-700 mb-3 font-medium transition">Sign Up</button>
      <button onclick="signIn()" class="w-full bg-gray-100 text-gray-700 py-3 rounded-lg hover:bg-gray-200 mb-4 font-medium transition">Login</button>
      <button onclick="signInWithGoogle()" class="w-full bg-white border-2 border-gray-200 text-gray-700 py-3 rounded-lg hover:bg-gray-50 font-medium transition flex items-center justify-center">
        <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24">
          <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
          <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
          <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
          <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
        </svg>
        Sign in with Google
      </button>
      <p id="auth-status" class="text-center text-sm text-gray-600 mt-4"></p>
    </div>
  </section>

  <!-- Features Section -->
  <section id="features" class="py-20 bg-white">
    <div class="max-w-6xl mx-auto px-6 text-center">
      <h3 class="text-3xl font-bold text-gray-900 mb-12">Key Features</h3>
      <div class="grid md:grid-cols-3 gap-10">
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow duration-300">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mb-4 mx-auto">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
          <h4 class="text-xl font-semibold mb-3 text-gray-900">ICD-10 Extraction</h4>
          <p class="text-gray-600 leading-relaxed">Upload or paste consult notes. CodeCraftMD extracts diagnoses using medical NLP.</p>
        </div>
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow duration-300">
          <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mb-4 mx-auto">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
            </svg>
          </div>
          <h4 class="text-xl font-semibold mb-3 text-gray-900">Editable Review Panel</h4>
          <p class="text-gray-600 leading-relaxed">Easily review, refine, or override codes before submission.</p>
        </div>
        <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100 hover:shadow-md transition-shadow duration-300">
          <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mb-4 mx-auto">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
          <h4 class="text-xl font-semibold mb-3 text-gray-900">Billing Summary Generation</h4>
          <p class="text-gray-600 leading-relaxed">Create CMS-1500-style billing forms for print or export.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section id="how-it-works" class="py-20 bg-white">
    <div class="max-w-6xl mx-auto px-6">
      <h3 class="text-3xl font-bold text-gray-900 text-center mb-12">How It Works</h3>
      <div class="max-w-4xl mx-auto">
        <div class="grid md:grid-cols-2 gap-8">
          <div class="bg-gray-50 p-6 rounded-xl border border-gray-100">
            <div class="flex items-center mb-4">
              <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center font-bold mr-3">1</div>
              <h4 class="text-xl font-semibold text-gray-900">Log in securely to your dashboard</h4>
            </div>
            <p class="text-gray-600 ml-11">Access your secure, HIPAA-compliant CodeCraftMD workspace.</p>
          </div>
          <div class="bg-gray-50 p-6 rounded-xl border border-gray-100">
            <div class="flex items-center mb-4">
              <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center font-bold mr-3">2</div>
              <h4 class="text-xl font-semibold text-gray-900">Upload your consultation note</h4>
            </div>
            <p class="text-gray-600 ml-11">Paste text or upload PDF, DOCX, or TXT files directly.</p>
          </div>
          <div class="bg-gray-50 p-6 rounded-xl border border-gray-100">
            <div class="flex items-center mb-4">
              <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center font-bold mr-3">3</div>
              <h4 class="text-xl font-semibold text-gray-900">Review and confirm ICD-10 codes</h4>
            </div>
            <p class="text-gray-600 ml-11">AI extracts codes automatically. Review, edit, or add as needed.</p>
          </div>
          <div class="bg-gray-50 p-6 rounded-xl border border-gray-100">
            <div class="flex items-center mb-4">
              <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center font-bold mr-3">4</div>
              <h4 class="text-xl font-semibold text-gray-900">Download your billing summary</h4>
            </div>
            <p class="text-gray-600 ml-11">Generate and print professional CMS-1500 style billing forms.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gray-50 text-center">
    <div class="max-w-3xl mx-auto px-6">
      <div class="bg-white p-12 rounded-2xl shadow-sm border border-gray-100">
        <h3 class="text-3xl font-bold text-gray-900 mb-4">Simplify Your Clinical Billing Today</h3>
        <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">CodeCraftMD turns documentation into revenue—securely and intelligently.</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="#" class="bg-blue-600 text-white font-semibold px-8 py-4 rounded-xl hover:bg-blue-700 transition shadow-sm">
            Try CodeCraftMD Free →
          </a>
          <button onclick="window.showLeadMagnet && window.showLeadMagnet()" class="bg-green-600 text-white font-semibold px-8 py-4 rounded-xl hover:bg-green-700 transition shadow-sm">
            📚 Get Free AI Billing Guide
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="py-16 bg-gray-50">
    <div class="max-w-4xl mx-auto px-6 text-center">
      <h3 class="text-3xl font-bold text-gray-900 mb-8">Get in Touch</h3>
      <p class="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
        Ready to transform your medical billing workflow? Contact us to learn more about CodeCraftMD or schedule a personalized demo.
      </p>
      <div class="bg-white p-8 rounded-xl shadow-sm border border-gray-100 max-w-2xl mx-auto">
        <div class="grid md:grid-cols-2 gap-8">
          <div class="text-left">
            <h4 class="text-lg font-semibold text-gray-900 mb-3">Contact Information</h4>
            <div class="space-y-3">
              <div class="flex items-center">
                <svg class="w-5 h-5 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
                <a href="mailto:info@codecraftmd.com" class="text-blue-600 hover:text-blue-800 font-medium">
                  info@codecraftmd.com
                </a>
              </div>
              <div class="flex items-center">
                <svg class="w-5 h-5 text-blue-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
                <span class="text-gray-600">Dr. Chukwuma Onyeije, Founder</span>
              </div>
            </div>
          </div>
          <div class="text-left">
            <h4 class="text-lg font-semibold text-gray-900 mb-3">Quick Actions</h4>
            <div class="space-y-3">
              <button onclick="window.showLeadMagnet && window.showLeadMagnet()" class="block w-full bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 transition text-center font-medium">
                📚 Free Billing Guide
              </button>
              <a href="#" class="block bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition text-center font-medium">
                Schedule a Demo
              </a>
              <a href="#" class="block bg-gray-100 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-200 transition text-center font-medium">
                Start Free Trial
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-white border-t py-6 text-center text-sm text-gray-500">
    <div class="max-w-4xl mx-auto px-6">
      <p>© 2025 CodeCraftMD, Inc. | Created by Dr. Chukwuma Onyeije</p>
    </div>
  </footer>

  <!-- Firebase Auth Logic -->
  <script>
    const auth = firebase.auth();
    const provider = new firebase.auth.GoogleAuthProvider();

    function signUp() {
      const email = document.getElementById("email").value;
      const password = document.getElementById("password").value;
      auth.createUserWithEmailAndPassword(email, password)
        .then(userCredential => {
          document.getElementById("auth-status").innerText = "Sign-up successful!";
          window.location.href = "/dashboard.html"; // Redirect
        })
        .catch(error => {
          console.error("Sign-up error:", error.code, error.message);
          document.getElementById("auth-status").innerText = error.message;
        });
    }

    function signIn() {
      const email = document.getElementById("email").value;
      const password = document.getElementById("password").value;
      auth.signInWithEmailAndPassword(email, password)
        .then(userCredential => {
          document.getElementById("auth-status").innerText = "Login successful!";
          window.location.href = "/dashboard.html"; // Redirect
        })
        .catch(error => {
          console.error("Login error:", error.code, error.message);
          document.getElementById("auth-status").innerText = error.message;
        });
    }

    function signInWithGoogle() {
      auth.signInWithPopup(provider)
        .then(result => {
          const user = result.user;
          document.getElementById("auth-status").innerText = `Welcome, ${user.displayName}`;
          window.location.href = "/dashboard.html"; // Redirect
        })
        .catch(error => {
          console.error("Google Sign-In error:", error.code, error.message);
          document.getElementById("auth-status").innerText = error.message;
        });
    }

    auth.onAuthStateChanged(user => {
      if (user) {
        console.log("Authenticated as:", user.displayName || user.email);
      } else {
        console.log("User not logged in.");
      }
    });
    
    // Register service worker for PWA functionality
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js')
          .then((registration) => {
            console.log('ServiceWorker registration successful:', registration);
          })
          .catch((error) => {
            console.log('ServiceWorker registration failed:', error);
          });
      });
    }

    // Lead Magnet Popup Functionality
    document.addEventListener('DOMContentLoaded', function() {
      const modal = document.getElementById('leadMagnetModal');
      const modalContent = document.getElementById('modalContent');
      const closeBtn = document.getElementById('closeModal');
      const gumroadBtn = document.getElementById('gumroadButton');
      
      let popupShown = false;
      let scrollTriggered = false;
      
      // Check if user has already seen the popup (localStorage)
      if (localStorage.getItem('leadMagnetSeen')) {
        popupShown = true;
      }
      
      // Show popup after 15 seconds or 50% scroll
      setTimeout(() => {
        if (!popupShown && !localStorage.getItem('leadMagnetSeen')) {
          showPopup();
        }
      }, 15000); // 15 seconds
      
      // Show popup on scroll (50% of page)
      window.addEventListener('scroll', function() {
        if (!scrollTriggered && !popupShown && !localStorage.getItem('leadMagnetSeen')) {
          const scrollPercent = (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;
          if (scrollPercent > 50) {
            showPopup();
            scrollTriggered = true;
          }
        }
      });
      
      // Show popup function
      function showPopup() {
        popupShown = true;
        modal.classList.remove('hidden');
        modal.classList.add('flex');
        
        // Animate modal appearance
        setTimeout(() => {
          modalContent.classList.remove('scale-95', 'opacity-0');
          modalContent.classList.add('scale-100', 'opacity-100');
        }, 50);
      }
      
      // Close popup function
      function closePopup() {
        modalContent.classList.remove('scale-100', 'opacity-100');
        modalContent.classList.add('scale-95', 'opacity-0');
        
        setTimeout(() => {
          modal.classList.add('hidden');
          modal.classList.remove('flex');
        }, 300);
        
        // Remember that user has seen the popup
        localStorage.setItem('leadMagnetSeen', 'true');
      }
      
      // Event listeners
      closeBtn.addEventListener('click', closePopup);
      
      // Close on background click
      modal.addEventListener('click', function(e) {
        if (e.target === modal) {
          closePopup();
        }
      });
      
      // Close on Escape key
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && !modal.classList.contains('hidden')) {
          closePopup();
        }
      });
      
      // Track Gumroad button click
      gumroadBtn.addEventListener('click', function() {
        // Mark popup as seen and close it
        localStorage.setItem('leadMagnetSeen', 'true');
        closePopup();
        
        // Analytics tracking (you can add Google Analytics or other tracking here)
        console.log('Lead magnet clicked');
      });
      
      // Add manual trigger for testing (remove in production)
      window.showLeadMagnet = showPopup;
    });
  </script>
</body>
</html>
